class Model {
  constructor(Field, Head, Tail, Apple) {
    this.field = new Field();
    this.head = new Head();
    this.tail = new Tail();
    this.apple = new Apple();
  }
}

export default Model;
